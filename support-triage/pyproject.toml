[project]
name = "support-triage"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langgraph",
    "langchain-core",
    "anthropic",
    "pydantic",
    "httpx",
    "python-dotenv",
]